    <section class="container-fluid px-0 py-5 m-0">
        <div class="container">
            <h6 class="text-center">Ante cualquier duda o consulta puede enviarnos un correo a través del siguiente formulario: </h6>
            <div class="row p-0 m-0 justify-content-center">
                <div class="col-xl-6 col-lg-6 col-md-12 col-sm-12 col-xs-12 p-0 m-0">
                    <form action="/contacto/gracias" method="POST" id="form_contacto" subject="Consulta enviada desde la Web">
                        <div class="form-group p-0 m-0 ">
                            <label for="nombre"></label>
                            <input 
                            type="text" 
                            class="form-control campos" 
                            id="nombre" 
                            name="nombre"
                            placeholder="Nombre" 
                            required>
                        </div>

                        <div class="form-group p-0 m-0">
                            <label for="mail"></label>
                            <input 
                            type="email" 
                            class="form-control campos" 
                            id="mail" 
                            name="mail"
                            placeholder="Mail" 
                            pattern="^[_a-z0-9-]+(\.[_a-z0-9-]+)*@[a-z0-9-]+(\.[a-z0-9-]+)*(\.[a-z]{2,3})$" 
                            required>
                        </div>

                        <div class="form-group p-0 m-0">
                            <label for="asunto"></label>
                            <input 
                            type="text" 
                            class="form-control campos" 
                            id="asunto"
                            name="asunto" 
                            placeholder="Asunto" 
                            required>
                        </div>

                        <div class="form-group">
                            <label for="consulta"></label>
                            <textarea 
                            class="form-control campos" 
                            id="consulta" 
                            name="consulta"
                            placeholder="Escribe aquí tu consulta" 
                            rows="3"
                            ></textarea>
                        </div>

                        <div class="text-center pt-3">
                            <button type="submit" value="Submit" class="btn btn-primary w-25">Enviar</button>
                        </div>

                        <div class="pt-2 text-center">
                            <span class="text-success">{{message}}</span>
                        </div>

                    </form>
                </div>
            </div>
        </div>
    </section>